CLI option pricing calculator
Successfully tested on Ubuntu (with g++) and Win 11 (with Visual Studio).

In order to build just link all source files together, then run with '--help' for instruction.

